import styled from 'styled-components';

const MarginsWrapper = styled.span`
    margin-left : ${(props) => props.margin};
    margin-right: ${(props) => props.margin};
`;

export default MarginsWrapper;
