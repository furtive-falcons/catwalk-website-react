import styled from 'styled-components';

const StyleWrapper = styled.div(() => ({
  display: 'flex',
  flexWrap: 'wrap',
  gap: '.5rem',
  marginTop: '1rem',
}));

export default StyleWrapper;
